# 阿里云服务器部署指南 - 技术流程图项目

## 服务器信息
- **公网IP**: 60.205.152.208
- **操作系统**: 推荐 CentOS 7.x 或 8.x / Ubuntu 20.04 / Ubuntu 22.04
- **推荐工具**: 魔塔（宝塔面板）

---

## 第一步：连接到阿里云服务器

### 方式1：使用SSH工具（推荐）

#### Windows用户
1. 下载SSH工具：
   - **FinalShell**（推荐，免费）：https://www.hostbuf.com/
   - **Xshell**（收费）：https://www.netsarang.com/en/
   - **MobaXterm**（免费版）：https://mobaxterm.mobatek.net/

2. 配置连接：
   - **主机**: 60.205.152.208
   - **端口**: 22（SSH默认端口）
   - **用户名**: root（阿里云默认）
   - **密码**: 您购买服务器时设置的密码（或使用SSH密钥）

#### Mac/Linux用户
直接使用终端：
```bash
ssh root@60.205.152.208
# 输入密码后回车（密码不会显示）
```

---

## 第二步：安装魔塔（宝塔面板）

### 方法1：使用一键安装命令（推荐）

连接到服务器后，执行以下命令：

**CentOS安装命令：**
```bash
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh ed8484bec
```

**Ubuntu/Debian安装命令：**
```bash
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh ed8484bec
```

**安装过程说明：**
1. 询问是否安装到/www目录：输入 `y` 回车
2. 等待安装完成（约3-5分钟）
3. 安装成功后会显示：
   ```
   ==================================================================
   Congratulations! Installed successfully!
   ==================================================================
   外网面板地址: http://60.205.152.208:8888/xxxxxxxx
   内网面板地址: http://172.17.0.1:8888/xxxxxxxx
   username: xxxxxxxx
   password: xxxxxxxx
   ==================================================================
   ```
4. **重要**：保存上面的username和password！

### 方法2：手动安装（如果自动安装失败）

```bash
# 1. 下载安装包
wget https://download.bt.cn/install/install_6.0.sh

# 2. 添加执行权限
chmod +x install_6.0.sh

# 3. 执行安装
./install_6.0.sh ed8484bec
```

---

## 第三步：配置阿里云安全组

### 1. 登录阿里云控制台
1. 访问：https://ecs.console.aliyun.com/
2. 找到您的服务器（实例）
3. 点击实例ID进入详情页

### 2. 配置安全组规则
1. 点击左侧菜单：**安全组**
2. 点击：**配置规则** → **手动添加**
3. 添加以下端口：

| 端口范围 | 授权对象 | 协议类型 | 描述 |
|---------|---------|---------|------|
| 8888 | 0.0.0.0/0 | TCP | 魔塔面板端口 |
| 80 | 0.0.0.0/0 | TCP | HTTP |
| 443 | 0.0.0.0/0 | TCP | HTTPS |
| 22 | 0.0.0.0/0 | TCP | SSH（可选，不建议开放） |
| 8000 | 0.0.0.0/0 | TCP | 您的项目端口 |

4. 点击**保存**

### 3. 检查防火墙（如果是CentOS 7+）

```bash
# 查看防火墙状态
systemctl status firewalld

# 如果防火墙开启，需要添加端口
firewall-cmd --zone=public --add-port=8888/tcp --permanent
firewall-cmd --zone=public --add-port=8000/tcp --permanent
firewall-cmd --reload
```

---

## 第四步：登录魔塔面板

### 1. 访问魔塔面板
在浏览器中打开：
```
http://60.205.152.208:8888/xxxxxxxx
```
（使用安装成功后显示的地址）

### 2. 输入用户名和密码
- 使用安装成功后显示的username和password登录

### 3. 绑定宝塔账号
1. 首次登录会提示绑定手机号
2. 输入手机号获取验证码
3. 绑定成功

---

## 第五步：在魔塔中安装环境

### 1. 安装Nginx（用于运行HTTP服务器）

在魔塔面板中：
1. 点击左侧菜单：**软件商店**
2. 搜索：**Nginx**
3. 点击：**安装**
4. 选择版本：**1.22** 或 **最新稳定版**
5. 点击：**提交**
6. 等待安装完成（约2-3分钟）

### 2. 安装Python（用于运行简单的HTTP服务器，可选）

如果不需要Nginx，也可以使用Python的简单HTTP服务器：

1. 点击左侧菜单：**软件商店**
2. 搜索：**Python项目管理器**
3. 点击：**安装**
4. 或者直接在终端中：
   ```bash
   # CentOS
   yum install -y python3

   # Ubuntu
   apt-get install -y python3
   ```

---

## 第六步：上传项目文件到服务器

### 方法1：使用魔塔的文件管理功能（最简单）

1. 在魔塔面板中，点击左侧菜单：**文件**
2. 进入目录：**www/wwwroot**（Nginx默认网站目录）
3. 点击：**新建目录**，输入项目名称，如：`tech-flow`
4. 进入新建的目录，点击：**上传**
5. 选择您的本地文件：`index.html`
6. 上传完成

### 方法2：使用FTP工具（推荐批量上传）

1. **下载FTP工具**：
   - **FileZilla**（免费，推荐）：https://filezilla-project.org/
   - **WinSCP**（免费）：https://winscp.net/

2. **配置FTP连接**：
   - **主机**: 60.205.152.208
   - **端口**: 21
   - **用户名**: root
   - **密码**: 您的服务器密码

3. **在魔塔中安装FTP服务**：
   - 在魔塔面板中：**软件商店** → 搜索：**Pure-Ftpd** → **安装**
   - 安装完成后，在**网站**菜单中可以查看FTP信息

4. **上传文件**：
   - 连接FTP后，导航到 `/www/wwwroot/tech-flow/`
   - 拖拽 `index.html` 文件到服务器

### 方法3：使用Git（推荐，适合持续更新）

1. 在魔塔面板中，点击：**终端**
2. 安装Git：
   ```bash
   # CentOS
   yum install -y git

   # Ubuntu
   apt-get install -y git
   ```

3. 克隆您的GitHub仓库：
   ```bash
   cd /www/wwwroot
   git clone https://github.com/catforpy/business-process-flowchart.git tech-flow
   ```

4. 后续更新：
   ```bash
   cd /www/wwwroot/tech-flow
   git pull origin main
   ```

---

## 第七步：配置Nginx网站

### 1. 在魔塔中创建网站

1. 点击左侧菜单：**网站**
2. 点击：**添加站点**
3. 填写信息：
   - **域名**：填写IP地址 `60.205.152.208` 或您的域名
   - **根目录**：选择 `/www/wwwroot/tech-flow`
   - **PHP版本**：选择**纯静态**
4. 点击：**提交**

### 2. 修改网站配置（如果需要）

1. 在网站列表中，找到刚创建的网站
2. 点击：**设置** → **配置文件**
3. 修改端口（如果需要改为8000）：
   ```nginx
   server {
       listen 8000;  # 改为8000端口
       server_name 60.205.152.208;

       root /www/wwwroot/tech-flow;
       index index.html;

       location / {
           try_files $uri $uri/ /index.html;
       }
   }
   ```
4. 点击：**保存**
5. 重启Nginx：在**软件商店**中找到Nginx，点击：**重载配置**

---

## 第八步：启动HTTP服务器

### 方法1：使用Nginx（推荐）

1. 在魔塔面板中，找到您的网站
2. 点击：**启动**
3. 访问：`http://60.205.152.208` 或 `http://60.205.152.208:8000`

### 方法2：使用Python简单HTTP服务器（测试用）

1. 在魔塔面板中，点击：**终端**
2. 进入项目目录：
   ```bash
   cd /www/wwwroot/tech-flow
   ```
3. 启动HTTP服务器：
   ```bash
   # 绑定到所有网卡，端口8000
   python3 -m http.server 8000 --bind 0.0.0.0
   ```
4. 后台运行（推荐）：
   ```bash
   nohup python3 -m http.server 8000 --bind 0.0.0.0 > /dev/null 2>&1 &
   ```

### 方法3：使用Nohup让服务持续运行

```bash
# 进入项目目录
cd /www/wwwroot/tech-flow

# 后台运行Python HTTP服务器
nohup python3 -m http.server 8000 --bind 0.0.0.0 > server.log 2>&1 &

# 查看进程
ps aux | grep "python3 -m http.server"

# 如果需要停止服务
kill [进程ID]
```

---

## 第九步：验证部署

### 1. 访问您的网站

在浏览器中打开：
```
http://60.205.152.208
```
或
```
http://60.205.152.208:8000
```

### 2. 检查是否正常运行

您应该能看到完整的流程图界面。

### 3. 测试各个功能

- 点击"展开/折叠所有详情"按钮
- 点击各个节点查看tooltip说明
- 测试响应速度

---

## 第十步：设置服务开机自启动（可选）

### 方法1：使用Systemd服务（推荐）

1. 创建服务文件：
   ```bash
   vim /etc/systemd/system/tech-flow.service
   ```

2. 添加以下内容：
   ```ini
   [Unit]
   Description=Tech Flow HTTP Server
   After=network.target

   [Service]
   Type=simple
   User=root
   WorkingDirectory=/www/wwwroot/tech-flow
   ExecStart=/usr/bin/python3 -m http.server 8000 --bind 0.0.0.0
   Restart=on-failure

   [Install]
   WantedBy=multi-user.target
   ```

3. 重载systemd并启动服务：
   ```bash
   systemctl daemon-reload
   systemctl enable tech-flow
   systemctl start tech-flow
   systemctl status tech-flow
   ```

### 方法2：使用Supervisor（推荐，魔塔面板可直接安装）

1. 在魔塔面板中：**软件商店** → 搜索：**Supervisor管理器** → **安装**
2. 添加守护进程：
   - **名称**: tech-flow
   - **启动用户**: root
   - **运行目录**: /www/wwwroot/tech-flow
   - **启动命令**: python3 -m http.server 8000 --bind 0.0.0.0
3. 点击：**提交**并**启动**

---

## 常见问题排查

### 问题1：无法访问魔塔面板（8888端口）

**解决方案：**
```bash
# 检查魔塔面板是否运行
bt status

# 重新启动魔塔面板
bt restart

# 查看默认端口
cat /www/server/panel/data/default.pl
# 查找 "default_port" 的值

# 如果需要重置密码
bt default
```

### 问题2：无法访问网站（80或8000端口）

**解决方案：**
```bash
# 1. 检查Nginx是否运行
systemctl status nginx

# 2. 启动Nginx
systemctl start nginx

# 3. 检查端口是否被占用
netstat -tunlp | grep 80
netstat -tunlp | grep 8000

# 4. 如果端口被占用，停止占用进程
kill [进程ID]

# 5. 查看Nginx日志
tail -f /www/wwwlogs/access.log
```

### 问题3：安全组规则无效

**解决方案：**
1. 检查服务器内部防火墙：
   ```bash
   # CentOS 7
   firewall-cmd --list-all
   systemctl status firewalld

   # Ubuntu
   ufw status
   ```

2. 如果防火墙开启，需要添加端口：
   ```bash
   # CentOS 7
   firewall-cmd --permanent --add-port=8000/tcp
   firewall-cmd --reload

   # Ubuntu
   ufw allow 8000/tcp
   ufw reload
   ```

### 问题4：文件上传后无法访问

**解决方案：**
1. 检查文件权限：
   ```bash
   cd /www/wwwroot/tech-flow
   ls -l index.html
   ```

2. 修改权限：
   ```bash
   chmod 644 index.html
   chown www:www index.html
   ```

3. 检查Nginx配置：
   ```bash
   # 测试Nginx配置
   nginx -t

   # 重载Nginx配置
   nginx -s reload
   ```

---

## 推荐的部署架构（生产环境）

### 架构方案1：使用Nginx（推荐生产环境）

```
用户浏览器
    ↓
阿里云ECS (60.205.152.208)
    ├─ Nginx (端口80/443)
    │   ├─ 反向代理
    │   ├─ SSL证书
    │   └─ 静态文件服务
    ├─ 项目文件
    │   └─ /www/wwwroot/tech-flow/index.html
    └─ 防火墙
        ├─ 开放端口: 80, 443, 8888
        └─ 限制: 只允许必要端口
```

### 架构方案2：使用Docker（推荐容器化部署）

```bash
# 1. 安装Docker（在魔塔面板中：软件商店 → Docker → 安装）

# 2. 创建Dockerfile
cat > Dockerfile <<EOF
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
EOF

# 3. 构建镜像
docker build -t tech-flow .

# 4. 运行容器
docker run -d -p 80:80 --name tech-flow tech-flow

# 5. 后台持续运行（自动重启）
docker run -d -p 80:80 --name tech-flow --restart=always tech-flow
```

---

## 监控和维护

### 1. 使用魔塔的监控功能

在魔塔面板中：
1. 点击：**监控**
2. 查看系统资源使用情况（CPU、内存、磁盘、网络）
3. 设置告警阈值

### 2. 定期备份数据

在魔塔面板中：
1. 点击：**计划任务**
2. 添加备份任务：
   - **任务类型**：备份网站
   - **执行周期**：每天凌晨3点
   - **保留份数**：7天

### 3. 查看访问日志

```bash
# 实时查看访问日志
tail -f /www/wwwlogs/access.log

# 查看错误日志
tail -f /www/wwwlogs/error.log

# 查看最近的访问
tail -100 /www/wwwlogs/access.log
```

---

## 安全建议

### 1. 修改SSH端口（推荐）

```bash
# 编辑SSH配置
vim /etc/ssh/sshd_config

# 找到并修改：
# Port 22 → Port 22222

# 重启SSH服务
systemctl restart sshd

# 记得在阿里云安全组中添加新端口22222
```

### 2. 禁用root远程登录（推荐）

```bash
# 1. 创建新用户
adduser admin
passwd admin

# 2. 给新用户sudo权限
visudo
# 添加：admin ALL=(ALL:ALL) ALL

# 3. 编辑SSH配置
vim /etc/ssh/sshd_config

# 修改：
# PermitRootLogin yes → PermitRootLogin no

# 4. 重启SSH服务
systemctl restart sshd
```

### 3. 配置SSL证书（推荐）

在魔塔面板中：
1. 点击：**网站** → 选择您的网站 → **设置** → **SSL**
2. 选择：**Let's Encrypt**（免费）或上传您自己的证书
3. 点击：**申请**或**上传**
4. 开启**强制HTTPS**

### 4. 定期更新系统和软件

```bash
# CentOS更新
yum update -y

# Ubuntu更新
apt-get update && apt-get upgrade -y

# 更新魔塔面板
bt update
```

---

## 总结

### 快速部署步骤（5分钟）

1. **连接服务器**：`ssh root@60.205.152.208`
2. **安装魔塔**：`yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh ed8484bec`
3. **配置安全组**：在阿里云控制台开放端口 8888, 80, 8000
4. **登录魔塔**：访问 `http://60.205.152.208:8888/xxxxxxxx`
5. **安装Nginx**：在魔塔面板中安装
6. **上传文件**：使用文件管理功能上传 `index.html` 到 `/www/wwwroot/tech-flow/`
7. **访问网站**：`http://60.205.152.208`

### 生产环境部署建议

1. ✅ 使用魔塔面板管理
2. ✅ 使用Nginx提供HTTP服务
3. ✅ 配置SSL证书（HTTPS）
4. ✅ 设置防火墙和安全组
5. ✅ 定期备份数据
6. ✅ 使用Systemd或Supervisor管理服务进程
7. ✅ 配置监控告警
8. ✅ 定期更新系统

祝您部署顺利！🎉
